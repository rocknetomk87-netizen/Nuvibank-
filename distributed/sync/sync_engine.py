class SyncEngine:

    @staticmethod
    def synchronize(

        source,

        target,

        data
    ):

        print(

            f"[SYNC] {source} -> {target}",

            data
        )

        return True
